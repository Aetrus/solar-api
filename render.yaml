services:
  - type: web
    name: solarestimate-pro
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api.main:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PORT
        value: 10000
      - key: GOOGLE_MAPS_API_KEY
        sync: false
